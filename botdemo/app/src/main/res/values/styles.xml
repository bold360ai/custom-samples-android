<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:dropDownListViewStyle">@style/demoDropDown</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:colorPrimaryDark" tools:targetApi="lollipop">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="demoDropDown" parent="Widget.AppCompat.Light.ListView.DropDown">
        <item name="android:dividerHeight">10dip</item>
        <item name="android:divider">@drawable/persistent_divider</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="settingsSection" parent="main_button">
        <item name="android:background">@drawable/settings_background</item>
    </style>

</resources>
